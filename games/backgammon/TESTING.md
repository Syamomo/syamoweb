# 確認結果

実施日：2026-09-24（日本時間）。

## 自動テスト

実行コマンド（SYAMOWEBルート）：

```powershell
& 'C:\Users\SYAMO\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe' --test games/backgammon/tests/engine.test.js
```

**35件成功、失敗0件。** `engine.js` と `app.js` の `node --check` も成功。

- 標準の初期配置と両者15枚の保持。
- 先攻決めの同点振り直し、大小による先攻、最初の2個の出目の利用。
- 不正な出目、振り直し、手番外の操作の拒否。
- 両者の移動方向、相手2枚によるブロック、相手1枚のヒット。
- バーの優先、両者の復帰地点、復帰時のヒット、複数枚の復帰、復帰後に残った目の利用、戻れない場合のパス。
- 使える出目を最大数使うための先読み。先に大きい目で復帰すると小さい目が使えなくなる局面では、小さい目からの復帰を強制。
- 片方だけ使用できるときの大きい目の強制、大きい目が使えない場合の小さい目の許可。
- ゾロ目4回、途中で進めなくなった場合の最大回数の利用。
- ベアオフの開始条件、ちょうどの出目、大きすぎる出目、両者のホーム、途中地点のブロック。
- ヒット・復帰・上がり・複数手のUndo。盤・バー・上がり枚数・未使用の目を完全復元。
- 手番確定前のUndo許可、確定後の禁止、出目の振り直し禁止。
- 最後の1枚を上げたあとも勝利確定前ならUndo可能。
- 通常勝ち、ギャモン、バックギャモン。
- 状態とUndo履歴の非破壊更新、公平な出目生成のための乱数バイトの棄却処理。
- 固定シードの12局を終局まで実行し、各移動のUndoによる完全復元、各色15枚の保存、勝者が15枚上げたことを確認。

## アプリ内ブラウザー

`http://127.0.0.1:4180/games/backgammon/` にて実施。ブラウザー画面に表示された操作対象を選択して対局しています。テスト用の局面注入や出目の固定は行っていません。

- 初期配置、上下の操作欄と黒側の180度回転、白黒の手番切り替え。
- タップに相当するクリックで駒選択、移動先の表示、移動。
- 通常の移動後にUndoし、駒と出目が戻ること。
- 1局を91手番・199移動で終局まで操作。ヒット22回、バーからの復帰24回、両者合計29枚のベアオフを含みます（復帰時のヒットは復帰に集計、Undo後の再操作は移動数に含めない）。
- ヒット・復帰・ベアオフについて各1回、Undo前後の盤の全ボタンの枚数表示と出目表示が元に戻ることを照合。
- 「白の勝ち！」「通常勝ち」、白15枚・黒14枚の上がり表示、両側の「もう一局」。
- 「もう一局」で15枚ずつの初期配置と先攻決定へ戻ること。
- 再戦の先攻決めで5と5が出た際、「同じ目。もう一度！」を表示し、その後の振り直しで進行すること。
- 遊び方の開閉、リセット確認の取消後に、局面・出目・手番が変わらないこと。
- Enterによる先攻決めと駒選択、Spaceによる移動。
- リセット確認を実行すると、初期配置・先攻未定・Undo不可へ戻ること。
- コンソールのerror / warningは0件。

## 画面サイズ

次のサイズで、ページ全体と両操作欄に横方向のはみ出しがないことをDOMの寸法でも確認しました。リサイズ後も対局は保持されました。

| 幅 × 高さ | 想定 |
| --- | --- |
| 320 × 740 | 小型スマホ縦画面。ゾロ目4個の表示も確認 |
| 390 × 844 | スマホ縦画面 |
| 568 × 320 | 小型スマホ横画面 |
| 667 × 375 | スマホ横画面 |
| 844 × 390 | スマホ横画面 |
| 1200 × 850 | PC |

縦画面、844×390の横画面、PCの対局中・勝利時をスクリーンショットで目視しました。検証用の画面サイズ指定は最後に解除しています。

## 未確認の範囲

- 物理的なスマートフォンのChrome上でのタッチ操作、ブラウザーのアドレスバー伸縮、実機での押しやすさ。
- 実際の2人が向かい合って遊んだときの操作感。
- 全ての特殊ルールのブラウザー再現。ルールの細かな分岐と最後の1枚のUndoは自動テストで検証しています。
- トップページの作品カード追加とインターネット公開。

この記録は自動テストおよびアプリ内ブラウザーで確認できた範囲を示すもので、実機Chromeでの検証を表すものではありません。
